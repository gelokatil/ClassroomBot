apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "classroombotapp.fullname" . }}-test-connection"
  labels:
    {{- include "classroombotapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "classroombotapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
